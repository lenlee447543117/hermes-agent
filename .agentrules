# AI沪老 V2.0 项目开发护栏规范
# 本文件为AI代码生成工具（Cursor/Claude Code CLI）的强制开发规范
# 所有AI生成代码必须遵循以下前置校验规则

## 项目基本信息
- 项目名称: AI沪老 (AI Hu Lao) V2.0
- 技术栈: Android(Kotlin+Compose) + Hermes后端(Python+FastAPI)
- 目标用户: 75岁以上高龄老人 + 30-55岁子女用户
- 核心原则: 适老化优先、安全第一、极简交互

## 编程语言规范
- Android客户端: Kotlin 1.9.20+，全面摒弃Java
- 异步处理: Kotlin Coroutines，禁止使用Thread
- UI框架: Jetpack Compose声明式组件
- Hermes后端: Python 3.10+，asyncio异步框架
- 所有Python代码必须添加Type Hints类型注解

## 架构规范
- Android严格遵循MVI架构模式(ViewState/ViewEvent/ViewEffect)
- 所有外部服务通过Hilt Module注入，禁止直接实例化
- Hermes后端采用FastAPI + Pydantic模型校验

## 安全规范（最高优先级）
- 禁止硬编码API Key、密码、Token等敏感信息
- 所有API Key必须通过BuildConfig(local.properties)或环境变量获取
- 用户隐私数据必须加密存储(SQLCipher/Fernet)
- 禁止在日志中输出用户隐私信息
- 端侧截屏数据上传前必须经过隐私脱敏处理

## 适老化UI强制规范
- 主色: #FF9500(暖阳橙)，背景色: #FFF8F0(暖米色)
- 基准字号: 24sp，禁用px单位，统一使用sp
- 所有按钮点击必须触发长震感触觉反馈
- 交互反馈: 语音+震动+视觉三重反馈
- 防误触: 所有危险操作需二次确认

## 自动化执行规范
- 操作延时: 所有触控操作添加300ms-600ms随机延迟
- 坐标校准: VLM返回坐标必须通过比例公式换算
- 截屏规范: MediaProjection截屏压缩至720p、70%质量、WebP格式
- 异常兜底: 所有操作必须有超时、失败、权限异常的降级方案

## 代码复用规则
- 生成任意功能代码前，优先检索现有工具类、通用组件
- 严禁重复造轮子，同类功能必须复用已有实现
- 通用组件放置于 ui/components/ 目录

## 注释规范
- 所有类必须添加头部注释: 所属业务模块、核心能力
- 核心方法必须添加注释: 功能说明、参数说明、返回值说明
- 禁止无意义注释，注释必须提供增量信息

## 单元测试规则
- 每个业务UseCase必须配套至少1组单元测试
- 测试必须覆盖: 正常流程、断网场景、空返回、识别失败
- 测试文件放置于对应模块的test/目录

## 合规校验规则
- 自动校验UI配色是否符合适老化规范
- 自动校验字号是否使用sp单位
- 自动检测硬编码敏感信息
- 自动检测交互话术是否符合适老化风格

## 跨端数据契约
- Android端与Hermes后端通讯统一使用JSON Schema
- 意图指令标准数据包: {traceId, timestamp, type, payload}
- VLM视觉识别标准请求包: {userId, screenSnapshot, query, context}
- 统一错误码: 1001(语音识别失败) 1002(意图路由冲突) 2001(自动化超时) 4001(实例启动失败)

## 方言交互规范
- 默认使用上海方言交互
- 方言话术必须亲切生活化，像家人聊天
- 异常安抚话术统一使用方言
- 禁止使用生硬的机器语言回复老人
